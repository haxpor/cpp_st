#pragma once

// Forward declare reference type
// Note: As reference type is basically pointer behind the scene, so it's similar to pointer type and we can forward declare it.
class Lib2Widget;

void PrintLib2Widget(Lib2Widget& w);
